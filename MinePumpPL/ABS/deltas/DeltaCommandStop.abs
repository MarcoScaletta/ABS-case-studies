delta DeltaCommandStop{

    modifies MinePump {

        /*@
          @ requires !pumpRunning;
          @ ensures !pumpRunning && !systemActive;
          @ */
        adds Unit stopSystem() {
            if (pumpRunning) {
                deactivatePump();
            }
            systemActive = False;
        }
    }
}